---
'pixi-charts': patch
---

Fill in the real GitHub URL (`sirajsharma/pixi-charts`) for the `homepage`, `repository`, and `bugs` package.json metadata fields, replacing the previous `TODO` placeholder. No code or API change — only the published package's links are affected.
